package sourceCode.resManager.resNode.Events
{
	import flash.events.Event;
	
	import sourceCode.resManager.resNode.model.Pack;

	public class PackSearchEvent extends Event
	{
		public var model:Pack; 
		
		public function PackSearchEvent(type:String,model:Pack)
		{
			super(type);
			this.model = model;
		}
	}
}