package sourceCode.resManager.resNode.Events
{
	import flash.events.Event;
	
	import sourceCode.resManager.resNode.model.FrameSlot;
	
	
	public class slotSearchEvent extends Event
	{
		public var model:FrameSlot; 
		
		public function slotSearchEvent(type:String,model:FrameSlot)
		{
			super(type);
			this.model = model;
		}
	}
}